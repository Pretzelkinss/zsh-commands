# Git whoami macro

alias gitwhoami='git config --list'

# Refresh commands - immediately refresh aliases and functions which are linked to your .zshrc file.

alias refresh-commands='source ~/.zshrc'

# Change aliases

alias change-aliases='nano /home/adminstrator/.zsh_aliases/public_aliases.zsh'
